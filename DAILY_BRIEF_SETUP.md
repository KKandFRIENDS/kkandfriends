# 데일리 마켓 브리핑 — 오너 설정 가이드

매일 아침(평일) **글로벌 마켓 브리핑**이 라운지(`/voices`)에 KK 명의로 자동 발행되고,
알림을 켠 승인 멤버에게 **사이트 알림 🔔 + 텔레그램**이 갑니다.

- **발행 시각:** 평일(월~금) 아침 **7시 (한국시간)**
- **글쓴이:** KK (관리자 계정 명의)
- **카테고리:** 시장/매크로
- **공개 범위:** 멤버 전용 (라운지). 비회원·심사중인 사람은 볼 수 없습니다.
- **알림:** 기본 **켜짐**. 멤버가 `/me → 프로필 수정`에서 끌 수 있습니다.
- **이메일은 보내지 않습니다** (매일 오는 메일은 피로하므로 제외).

아래 **1 → 2 → 3 → 4** 순서대로만 하시면 됩니다. 전부 합쳐 15분쯤 걸립니다.

---

## 1단계. 데이터베이스 준비 (2분)

브리핑을 저장하고 "누가 알림을 원하는지" 기억할 자리를 만드는 작업입니다.

1. 브라우저에서 **[supabase.com](https://supabase.com)** 접속 → 로그인 → 프로젝트 **`kkandfriends`** 클릭.
2. 왼쪽 메뉴에서 **SQL Editor** 클릭 → 오른쪽 위 **＋ New query** 클릭.
3. 이 저장소의 **`db/migrations/012_daily_brief.sql`** 파일을 엽니다.
   내용을 **전부 복사**(Ctrl+A → Ctrl+C)해서 SQL 편집기 빈칸에 **붙여넣기**(Ctrl+V).
4. 오른쪽 아래 초록색 **Run** 버튼 클릭.
5. 아래에 **"Success. No rows returned."** 라고 나오면 성공입니다. ✅

> 혹시 빨간 에러가 나오면 그 메시지를 저에게 그대로 복사해서 보내주세요.

---

## 2단계. AI 열쇠 발급 — Google Gemini (3분, 카드 불필요) ⭐ 현재 사용 중

브리핑 글을 실제로 써주는 AI 열쇠입니다. **카드 등록이 필요 없습니다.**
Gemini API는 하루 1,500회까지 무료인데 우리는 **하루 1회**만 쓰기 때문에 계속 무료입니다.

1. **[aistudio.google.com/apikey](https://aistudio.google.com/apikey)** 접속 (구글 계정 로그인).
2. **`Create API key`** 클릭 → 프로젝트를 고르라고 하면 아무거나 선택 → 생성.
3. 나온 키를 **복사 버튼**으로 복사하고 메모장에 보관.
4. 3단계에서 **`GEMINI_API_KEY`** 라는 이름으로 넣습니다.

> 사용 모델은 `gemini-3.6-flash`(무료 등급) 입니다. 혹시 계정에서 막히면 코드가 자동으로
> `gemini-3.5-flash` → `gemini-2.5-flash` 순서로 다시 시도하므로 발행이 멈추지 않습니다.

<details>
<summary>Claude로 쓰고 싶다면 (선택 — 유료)</summary>

### 방법 A. Vercel AI Gateway

브리핑 글을 실제로 써주는 AI를 쓰기 위한 열쇠입니다.
**Anthropic 사이트에 카드를 등록할 필요가 없습니다.** Vercel이 대신 중계해 주고,
결제 이력이 없는 계정에는 **30일마다 $5 크레딧을 무료로** 줍니다.
우리가 쓰는 양은 **월 약 $1.2** 이므로 계속 무료 범위 안입니다. (Claude 품질은 동일)

1. **[vercel.com](https://vercel.com)** 접속 → 로그인.
2. 화면 위쪽 메뉴에서 **AI Gateway** 클릭.
   (안 보이면 주소창에 **`vercel.com/ai-gateway`** 를 직접 입력하세요.)
3. **API Keys** 탭 클릭 → **Create Key** 클릭 → 이름 `kkandfriends-daily-brief` → 생성.
4. 만들어진 키를 **복사** 해 둡니다. (`vck_...` 처럼 생긴 긴 문자열)

⚠️ **주의 (2026-07-25 확인):** AI Gateway의 $5 무료 크레딧은 **카드로 본인 확인을 해야 풀립니다.**
카드 등록 없이 키만 만들면 호출 시 `401 Authentication failed` 가 납니다.

### 방법 B. Anthropic 직접
[console.anthropic.com](https://console.anthropic.com) → **Billing** 충전 → **API Keys** →
`sk-ant-...` 복사 → **`ANTHROPIC_API_KEY`** 이름으로 넣기.
카드가 막히는 흔한 원인은 **카드사의 해외결제 차단**입니다 (카드사 앱에서 허용 후 재시도).

**우선순위:** `GEMINI_API_KEY` → `AI_GATEWAY_API_KEY` → `ANTHROPIC_API_KEY` 순으로 코드가
자동 선택합니다. Claude로 바꾸고 싶으면 `GEMINI_API_KEY` 를 지우기만 하면 됩니다.
</details>

---

## 3단계. Vercel에 열쇠 넣기 (3분)

1. **[vercel.com](https://vercel.com)** → 프로젝트 **`kkandfriends`** 클릭.
2. 위쪽 탭에서 **Settings** 클릭 → 왼쪽 메뉴에서 **Environment Variables** 클릭.
3. 아래 **한 줄만** 새로 추가합니다.

   | 칸 | 넣을 값 |
   |---|---|
   | **Key** (이름) | `GEMINI_API_KEY` |
   | **Value** (값) | 2단계에서 복사한 키 전체 |
   | **Environment** | **Production** 체크 (기본값 그대로 두면 됩니다) |

   → **Save** 클릭.

4. 나머지 필요한 값(`SUPABASE_SERVICE_ROLE_KEY`, `CRON_SECRET`, `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID`)은
   **이미 들어가 있습니다.** 건드리지 마세요.

5. 위쪽 탭 **Deployments** → 맨 위 배포 오른쪽 **··· (점 세 개)** → **Redeploy** → **Redeploy** 확인.
   (새 열쇠를 읽게 하려면 한 번 다시 배포해야 합니다. 1~2분 걸립니다.)

---

## 4단계. 테스트 — 진짜 발행하기 전에 미리보기 (5분)

**먼저 미리보기(발행 안 함)** 를 해봅니다. 브라우저 주소창에 아래를 붙여넣고 Enter:

```
https://www.kkandfriends.com/api/cron/daily-brief?key=여기에_CRON_SECRET&dry=1
```

- `여기에_CRON_SECRET` 은 Vercel의 `CRON_SECRET` 값으로 바꿔주세요
  (Vercel → Settings → Environment Variables → `CRON_SECRET` 옆 눈 아이콘으로 확인).
- 화면에 `"dry": true` 와 함께 **`title`**(제목)과 **`body`**(본문)가 글자로 보입니다.
  `"via"` 값으로 어느 경로를 썼는지 확인할 수 있습니다 (`gemini` / `vercel-ai-gateway` / `anthropic`).

> 💡 **주말에 테스트할 때는 주소 끝에 `&force=1` 을 붙이세요.** 평일에만 발행하는 규칙 때문에
> 토·일에는 `{"skipped":"weekend"}` 만 나옵니다.
  이게 오늘 발행될 글입니다. **이 단계에서는 라운지에 아무것도 올라가지 않고 알림도 가지 않습니다.**
- 글 톤이 마음에 안 들면 저에게 말씀해 주세요. 프롬프트를 고치면 됩니다.

**마음에 들면 실제로 한 번 발행해 봅니다:**

```
https://www.kkandfriends.com/api/cron/daily-brief?key=여기에_CRON_SECRET&force=1
```

- `"ok": true` 와 함께 `url` 이 나옵니다. 그 주소를 열면 라운지에 올라간 글이 보입니다.
- `/notifications` 에 📈 알림이 와 있고, 텔레그램에도 요약이 옵니다.
- `force=1` 은 "주말이어도, 오늘 이미 발행했어도 그냥 해라"는 뜻입니다. 테스트용입니다.

이후로는 **평일 아침 7시에 자동으로** 돌아갑니다. 아무것도 안 하셔도 됩니다.

> ⚠️ Vercel 무료(Hobby) 플랜은 크론 시각이 정확히 7:00이 아니라 **7시~8시 사이**에 실행될 수 있습니다.
> 정확한 시각이 필요하면 Pro로 올려야 합니다.

---

## 확인해 주실 것 (중요)

브리핑은 **KK 계정 명의**로 올라갑니다. 그래서 KK님 본인 프로필이 **승인(approved)** 상태여야
라운지에서 글쓴이 이름이 "KK"로 뜹니다. 안 그러면 "멤버"라고 표시됩니다.

1. 관리자 구글 계정으로 로그인한 상태에서 **`/me`** 접속.
2. 이름이 **KK**(원하시는 표시 이름)이고 상태가 **승인됨**인지 확인.
3. 만약 **심사 중**이면 → **`/admin-members`** 에서 본인을 **승인** 클릭.

---

## 나중에 바꾸고 싶을 때

| 바꾸고 싶은 것 | 방법 |
|---|---|
| **발행 시각** | `vercel.json`의 `"0 22 * * 0-4"` 를 수정 (UTC 기준. 한국시간 −9시간. 예: 아침 8시 → `0 23 * * 0-4`) |
| **주말에도 발행** | 같은 줄을 `"0 22 * * *"` 로 |
| **글이 너무 길다/짧다** | `api/cron/daily-brief.js` 맨 아래 `## 길이` 부분의 글자 수 수정 |
| **톤 수정** | 같은 파일의 `SYSTEM_PROMPT` 수정 |
| **비용 줄이기 / 더 빠르게** | Vercel 환경변수 `ANTHROPIC_MODEL` 추가 — 게이트웨이 사용 시 `anthropic/claude-sonnet-5`, 직접 사용 시 `claude-sonnet-5` (약 1/2 가격) |
| **크레딧 남은 양 확인** | Vercel → **AI Gateway** → 사용량/한도 화면에서 확인 |
| **멤버 전용 텔레그램 채널로 보내기** | 텔레그램에서 채널 생성 → 봇을 관리자로 추가 → Vercel 환경변수 `TELEGRAM_CHANNEL_ID` = `@채널주소` 추가 (없으면 KK님 개인 채팅으로 갑니다) |
| **알림 끄기 (멤버 개인)** | 멤버가 직접 `/me → 프로필 수정 → 체크 해제` |

## (선택) 한국은행 ECOS 통계 연결

두 브리핑은 원래 야후 시세와 구글 뉴스 제목만 봅니다 — **국내 금리와 매크로 지표가 통째로 빠져
있습니다.** 한국은행 ECOS 오픈API를 붙이면 기준금리·국고채(3년/10년)·CD·회사채·물가·경상수지·
외환보유액·가계신용이 브리핑의 배경 재료로 들어갑니다. 무료입니다.

**넣지 않아도 됩니다.** 키가 없으면 브리핑은 지금과 **정확히 똑같이** 돌아갑니다. 그 섹션만 빠집니다.

### 1. 인증키 발급

**[ecos.bok.or.kr](https://ecos.bok.or.kr)** → 상단 **오픈API** → **인증키 신청**. 즉시 발급됩니다.

### 2. Vercel에 넣기

Vercel → 프로젝트 `kkandfriends` → 왼쪽 **Environment Variables** → **Add Environment Variable**

| 칸 | 값 |
|---|---|
| **Key** | `ECOS_API_KEY` |
| **Value** | 발급받은 인증키 |
| **Environments** | Production |
| **Sensitive** | 켜기 |

저장 후 **Deployments → 맨 위 배포 ⋯ → Redeploy.** 환경변수는 **새 배포부터** 적용됩니다.

### 3. 제대로 붙었는지 확인

브라우저에서 (`여기에_CRON_SECRET` 을 실제 값으로 교체):

```
https://www.kkandfriends.com/api/ecos-probe?key=여기에_CRON_SECRET
```

- **`"ok": true`** → 연결 성공. `picked` 항목에 브리핑이 실제로 쓸 문장들이 보입니다.
- **`"error": "INFO-100…"`** → 키를 거부당했습니다. 붙여넣을 때 **앞뒤 공백이나 줄바꿈**이
  딸려 들어간 경우가 대부분입니다. Vercel에서 값을 다시 입력하고 Redeploy 하세요.
- **`"configured": false`** → 키가 이 배포에 없습니다. Redeploy를 안 하셨을 가능성이 큽니다.

> 이 주소는 **아무것도 발행하지 않고** AI도 호출하지 않습니다. 통계만 읽어서 보여줍니다.
> 몇 번을 열어도 안전합니다.

`all` 목록에 한국은행이 실제로 쓰는 지표 이름 100개가 전부 들어 있습니다. 그 화면을 저에게
보여주시면 어느 지표를 골라 쓸지 정확히 맞춰드리겠습니다 (`lib/ecos.js` 의 `PICKS`).

### 알아두실 것

- ECOS는 **실시간이 아닙니다.** 금리는 보통 하루 늦고(T+1), 물가·경상수지는 월 단위 발표입니다.
  그래서 브리핑에는 "오늘의 등락"이 아니라 **"최신 발표 수준 + 기준 시점"** 으로 들어가고,
  프롬프트가 모델에게 시점을 반드시 밝히도록 지시합니다.
- **원/달러와 코스피는 일부러 ECOS에서 안 가져옵니다.** 야후가 당일 등락까지 주는데 ECOS의
  더 오래된 값을 같이 주면 모델이 두 숫자를 헷갈립니다.
- **외국인/기관 수급은 ECOS에 없습니다.** 그건 KRX 쪽 데이터입니다. 이 연결로는 안 채워집니다.

## 문제가 생기면

- **실패하면 KK님 텔레그램으로 ⚠️ 경고 메시지가 자동으로 옵니다.** 그 메시지를 저에게 보여주세요.
- 같은 날 두 번 발행되는 일은 구조적으로 막혀 있습니다 (`daily_briefs` 테이블이 하루에 한 줄만 허용).
- 시장 데이터를 못 가져오면 헤드라인만으로 글을 쓰고, 둘 다 실패하면 **아무것도 발행하지 않고** 알려줍니다.
