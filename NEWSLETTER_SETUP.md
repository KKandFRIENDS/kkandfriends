# 회원 뉴스레터(Stibee) 연동 — 오너 설정

`/me → 프로필 수정`에 **"KK의 새 글을 이메일 뉴스레터로 받습니다"** 체크박스가 생긴다.
회원이 체크하면 그 회원의 이메일이 Stibee 주소록에 추가되고, 해제하면 빠진다.

- 주소록은 공개 구독 폼(홈페이지·글 하단)과 **같은 주소록**이다.
- Stibee로는 **이메일만** 보낸다. 필명·본명은 보내지 않는다.
- 기본값은 **체크 해제**다. 회원 60명 누구도 자동으로 등록되지 않는다 — 본인이 체크해야 한다.
- 아래 세 가지가 모두 끝나기 전에는 체크박스가 **화면에 나타나지 않는다** (잠자는 상태).

한 단계씩, 끝나면 다음으로.

---

## 1. VPS DB에 컬럼 추가 (1분)

VPS에서 서버 폴더(`compose.yaml`이 있는 곳)로 이동한 뒤:

```
docker compose exec -T postgres psql -U kkf -d kkf < db/005_newsletter_opt_in.sql
```

`ALTER TABLE` / `COMMIT`이 나오면 성공. 여러 번 실행해도 안전하다.

## 2. Stibee API 키와 주소록 번호 확인

1. Stibee 대시보드 → 워크스페이스 **설정 → API 키** → 새 키 발급 → 복사.
   (메뉴 이름은 스티비 화면 기준으로 확인 필요)
2. 주소록 번호는 **`487966`** (`KKandFriends_Members`, 워크스페이스의 유일한 주소록).
   2026-09-26 Zapier 연결로 `GET /v1/lists` 호출해 확인.

⚠️ API 키는 주소록 전체를 바꿀 수 있는 비밀번호다. 채팅·메일·레포에 붙여넣지 말 것.

## 3. VPS 환경변수에 넣고 재시작

`/opt/kkf-community/.env` 에 두 줄 추가:

```
STIBEE_API_KEY=<2-1에서 복사한 키>
STIBEE_LIST_ID=<2-2의 숫자>
```

그다음 API 컨테이너만 재시작:

```
docker compose up -d api
```

## 4. 확인

1. 사이트 로그인 → `/me` → 프로필 수정 → 뉴스레터 체크박스가 보이는지.
2. 체크 → 저장 → Stibee 주소록 구독자 수가 1 늘었는지.
3. 체크 해제 → 저장 → 다시 줄었는지.

저장 시 "뉴스레터 설정을 스티비에 반영하지 못했습니다"가 나오면 키·번호를 다시 확인한다.
해제 쪽만 실패하면 Stibee 삭제 API 형식 문제일 수 있다 — `server/src/stibee.js`의
`removeSubscriber` 한 곳만 고치면 된다 (빌드 환경에서 스티비 문서에 접근하지 못해 삭제 API 형식은 미확인).

---

## 확인된 Stibee API 사실 (2026-09-26, Zapier 연결로 실제 호출)

- 스탠다드 요금제에서 **v1 API는 동작**한다 (`GET /v1/lists` → 200).
- **v2 API는 엔터프라이즈 요금제 필요** (400 `UnsupportedAPI`). → 서버 코드는 v1을 쓴다.
- **이메일 생성 API(Create Empty Email)는 프로 요금제 필요.** → 스탠다드에서는 "새 글 → 뉴스레터 초안 자동 생성"이 안 된다.
  뉴스레터 본문은 Stibee 편집기에서 직접 만든다.
- 구독자 추가/삭제 API가 스탠다드에서 되는지는 아직 실제 호출로 확인하지 않았다.

## 알려진 한계

- **스티비 메일 하단의 "수신거부"를 누른 회원**은 Stibee에서는 빠지지만 `/me` 체크박스는 체크된 채로 남는다.
  양방향 동기화는 Stibee 웹훅이 필요하다 — 필요해지면 추가.
- 개인정보처리방침(`privacy.html`) §1 "뉴스레터 구독 · 이메일 · 뉴스레터 발송" 행과 §3 수탁자(스티비)는
  이미 있다. "회원 설정에서 선택한 경우"를 명시할지는 KK 판단 (방침 변경은 시행 7일 전 공지 조항 있음).
